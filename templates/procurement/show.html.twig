{% extends 'base.html.twig' %}

{% block title %}Procurement{% endblock %}

{% block body %}
    <h1>Procurement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ procurement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ procurement.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ procurement.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('procurement_index') }}">back to list</a>

    <a href="{{ path('procurement_edit', {'id': procurement.id}) }}">edit</a>

    {{ include('procurement/_delete_form.html.twig') }}
{% endblock %}
