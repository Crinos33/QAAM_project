{% extends 'base.html.twig' %}

{% block title %}OptionSurvey{% endblock %}

{% block body %}
    <h1>OptionSurvey</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_survey.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ option_survey.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ option_survey.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('option_survey_index') }}">back to list</a>

    <a href="{{ path('option_survey_edit', {'id': option_survey.id}) }}">edit</a>

    {{ include('option_survey/_delete_form.html.twig') }}
{% endblock %}
