{% extends 'base.html.twig' %}

{% block title %}ProcurementType{% endblock %}

{% block body %}
    <h1>ProcurementType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ procurement_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ procurement_type.name }}</td>
            </tr>
        </tbody>
    </table>
<button class="btn btn-dark">
    <a href="{{ path('procurement_type_index') }}">back to list</a></button>
    <button class="btn btn-dark">
        <a href="{{ path('procurement_type_edit', {'id': procurement_type.id}) }}">edit</a></button>

    {{ include('procurement_type/_delete_form.html.twig') }}
{% endblock %}
